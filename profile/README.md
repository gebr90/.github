bringing the world onchain
# Create a new file
echo "# My Project" > README.md

# Initialize git repo
git init

# Stage the file
git add README.md

# Make the first commit
git commit -m "Initial commit"

# Verify
git log --oneline
